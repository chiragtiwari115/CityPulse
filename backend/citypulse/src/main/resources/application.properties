# Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/citypulse?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false

# JWT
# For development, you can use a default secret, but generate a secure one for production
# Generate using: openssl rand -base64 32
app.jwt.secret=${APP_JWT_SECRET:NE55M3gxbFBJckFjWUdxSktWU0Q5OGdXQ2hUanZFZjU=}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:3600000}

# Mail (optional for development - email sending will fail gracefully if not configured)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Auth0 OAuth
auth0.domain=dev-2xgho7dwrynbwitj.us.auth0.com
auth0.client-id=Rgvhiktnu7joCnBxkZsG4ZsO7ZXFewZd
auth0.client-secret=UGzfx8QxQAXpU5UdTqo7BXxwo3N3hZfOkpIvD-Y8YflHPe0m6hVd0mSgkGcKhCEa
auth0.callback-url=${AUTH0_CALLBACK_URL:http://localhost:3000/callback}

# Upload limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000}
server.port=${SERVER_PORT:8081}
